# This is a Databricks asset bundle definition for spotify_dab.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: spotify_dab
  uuid: 2c2bc830-eba4-4be4-b4f6-c0b84c98b178

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-7405614744543034.14.azuredatabricks.net
    presets:
      source_linked_deployment: false

  prod:
    mode: production
    workspace:
      host: https://adb-7405614744543034.14.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/adarshdamarla@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/adarshdamarla@gmail.com/PROD/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: adarshdamarla@gmail.com
        level: CAN_MANAGE
